--- START OF FILE: AGENTS.md ---
# Workspace: s-natives

This workspace follows the 8xxxx Semantic Taxonomy defined in s-seed/00200-Workspace-Taxonomy/taxonomy.txt.

## Engine Usage

The NATVS Engine is a sovereign orchestrator that manages the fleet-aware execution lifecycle.

### Antigravity Agent Manager
- **Discovery:** The Agent Manager automatically discovers the engine via the global `AGENTS.md` registry.
- **Integration:** Utilizes SACP (Sovereign Agent Control Protocol) over UDP for telemetry and command dispatch.
- **Daemon Mode:** The engine can run as a background daemon listening for fleet instructions on UDP port `50989`.

### Gemini CLI
- **Delegation:** Tasks are delegated to the engine using simple positional shell arguments.
- **Pattern:** `natvs-engine.exe "<objective>" "<context_path>"`
- **Protocol:** Gemini CLI monitors the engine's output for progress updates and uses the `ask_user` tool for manual approval during critical NATVS phase transitions (e.g., Synthesis).

## Adding New Agents

To register a new agent within the fleet so it is recognized by both platforms:

### 1. Register in Global AGENTS.md
Add a new `### Agent Name` section to `C:\aCogSpaceSeed\AGENTS.md`. Include its Role, Responsibility, and Interface. This is the primary source of truth for both Antigravity and Gemini CLI.

### 2. Configure Gemini CLI Subagent (Optional)
If the agent requires specialized tool handling beyond simple shell commands:
- Create a markdown file in `.gemini/agents/<agent-name>.md`.
- Define its tools (e.g., `run_shell_command`, `read_file`) and system prompt in the YAML frontmatter.

### 3. Verify Discovery
- **Antigravity:** Open the **Agent Manager** panel; the agent should appear automatically if documented in `AGENTS.md`.
- **Gemini CLI:** Run `/agents reload` followed by `/agents list` to confirm the agent is available for delegation.

---

## 🛠️ Verification & Conformance Testing

To maintain the architectural integrity of the NATVS Engine and SACP transport layers, the following testing suites must be executed and remain green:

### 1. Engine Main & Daemon Tests
Validates the internal phases of the NATVS engine (Negotiation, Assimilation, Transformation, Verification, Synthesis) and the background telemetry daemon:
```powershell
# From the workspace root (C:\aCogSpaceSeed):
& "C:\aCogSpaceSeed\00flow\s-forge\92000-external-toolchains\go\bin\go.exe" test -v sov.fleet/s-natives/...
```

### 2. SACP Broker Watchdog & Stream-Swap Conformance
Validates the pooled warm-worker stream-swap protocol and the watchdog reset-on-task idle shutdown invariants:
* **`TestIdleShutdownWatchdog`**: Verifies that the broker watchdog timer shuts down after inactivity but correctly delays and resets upon metabolic goal arrival.
* **`TestStreamSwapLifecycle`**: Verifies the dynamic allocation and cleanup of parallel invocation streams while keeping the control stream hot.
```powershell
# From the workspace root (C:\aCogSpaceSeed):
& "C:\aCogSpaceSeed\00flow\s-forge\92000-external-toolchains\go\bin\go.exe" test -v sov.fleet/s-hydration/100-synthesis-engine/...
```

### 3. SACP Go-Native Command Twins A/B Benchmarks
Measures the performance improvements of Go-native command polyfills (`pkg/bash` in `s-fab-aides`) compared to spawning external host shell processes (PowerShell):
```powershell
# From the workspace root (C:\aCogSpaceSeed):
& "C:\aCogSpaceSeed\00flow\s-forge\92000-external-toolchains\go\bin\go.exe" test -v -run "TestABPerformanceComparison" sov.fleet/s-fab-aides/81000-active-source/pkg/bash/...
```
* **Performance Baseline:** Bypasses process startup allocation delays entirely, demonstrating a **>3,400x speedup** (microsecond vs. millisecond scale) for file scans and pattern matching.



--- END OF FILE: AGENTS.md ---

--- START OF FILE: cover ---
mode: set

--- END OF FILE: cover ---

--- START OF FILE: coverage.out ---
mode: set
sov.fleet/s-natives/natvs_engine.go:43.52,52.2 1 1
sov.fleet/s-natives/natvs_engine.go:55.84,63.2 5 1
sov.fleet/s-natives/natvs_engine.go:66.87,73.16 5 1
sov.fleet/s-natives/natvs_engine.go:73.16,74.25 1 1
sov.fleet/s-natives/natvs_engine.go:74.25,76.4 1 1
sov.fleet/s-natives/natvs_engine.go:77.3,77.13 1 0
sov.fleet/s-natives/natvs_engine.go:80.2,81.28 2 1
sov.fleet/s-natives/natvs_engine.go:81.28,82.20 1 1
sov.fleet/s-natives/natvs_engine.go:82.20,85.94 2 1
sov.fleet/s-natives/natvs_engine.go:85.94,87.5 1 1
sov.fleet/s-natives/natvs_engine.go:91.2,92.12 2 1
sov.fleet/s-natives/natvs_engine.go:96.75,100.42 3 1
sov.fleet/s-natives/natvs_engine.go:100.42,125.37 4 1
sov.fleet/s-natives/natvs_engine.go:125.37,127.89 2 1
sov.fleet/s-natives/natvs_engine.go:127.89,128.19 1 1
sov.fleet/s-natives/natvs_engine.go:128.19,130.6 1 1
sov.fleet/s-natives/natvs_engine.go:131.5,131.21 1 1
sov.fleet/s-natives/natvs_engine.go:131.21,134.124 3 1
sov.fleet/s-natives/natvs_engine.go:134.124,137.7 2 1
sov.fleet/s-natives/natvs_engine.go:138.6,138.16 1 1
sov.fleet/s-natives/natvs_engine.go:140.5,141.128 2 1
sov.fleet/s-natives/natvs_engine.go:141.128,143.6 1 1
sov.fleet/s-natives/natvs_engine.go:145.5,146.19 2 1
sov.fleet/s-natives/natvs_engine.go:146.19,148.6 1 0
sov.fleet/s-natives/natvs_engine.go:149.5,152.46 3 1
sov.fleet/s-natives/natvs_engine.go:152.46,153.46 1 1
sov.fleet/s-natives/natvs_engine.go:153.46,156.7 2 1
sov.fleet/s-natives/natvs_engine.go:159.5,159.16 1 1
sov.fleet/s-natives/natvs_engine.go:159.16,163.6 3 1
sov.fleet/s-natives/natvs_engine.go:164.5,164.15 1 1
sov.fleet/s-natives/natvs_engine.go:166.4,166.18 1 1
sov.fleet/s-natives/natvs_engine.go:166.18,168.5 1 0
sov.fleet/s-natives/natvs_engine.go:170.3,171.13 2 1
sov.fleet/s-natives/natvs_engine.go:175.2,177.12 3 1
sov.fleet/s-natives/natvs_engine.go:181.83,187.44 4 1
sov.fleet/s-natives/natvs_engine.go:187.44,188.50 1 1
sov.fleet/s-natives/natvs_engine.go:188.50,191.4 2 1
sov.fleet/s-natives/natvs_engine.go:192.3,203.33 3 1
sov.fleet/s-natives/natvs_engine.go:203.33,211.18 6 1
sov.fleet/s-natives/natvs_engine.go:211.18,212.29 1 1
sov.fleet/s-natives/natvs_engine.go:212.29,213.20 1 1
sov.fleet/s-natives/natvs_engine.go:213.20,214.50 1 1
sov.fleet/s-natives/natvs_engine.go:214.50,216.8 1 1
sov.fleet/s-natives/natvs_engine.go:217.12,219.46 2 1
sov.fleet/s-natives/natvs_engine.go:219.46,221.108 2 1
sov.fleet/s-natives/natvs_engine.go:221.108,222.23 1 1
sov.fleet/s-natives/natvs_engine.go:222.23,224.10 1 0
sov.fleet/s-natives/natvs_engine.go:225.9,225.25 1 1
sov.fleet/s-natives/natvs_engine.go:225.25,227.52 2 1
sov.fleet/s-natives/natvs_engine.go:227.52,229.11 1 0
sov.fleet/s-natives/natvs_engine.go:230.10,230.20 1 1
sov.fleet/s-natives/natvs_engine.go:232.9,232.55 1 1
sov.fleet/s-natives/natvs_engine.go:232.55,235.10 2 0
sov.fleet/s-natives/natvs_engine.go:236.9,236.19 1 1
sov.fleet/s-natives/natvs_engine.go:238.8,238.23 1 1
sov.fleet/s-natives/natvs_engine.go:238.23,240.9 1 0
sov.fleet/s-natives/natvs_engine.go:246.4,246.20 1 1
sov.fleet/s-natives/natvs_engine.go:246.20,248.5 1 1
sov.fleet/s-natives/natvs_engine.go:250.4,250.28 1 1
sov.fleet/s-natives/natvs_engine.go:250.28,252.13 2 1
sov.fleet/s-natives/natvs_engine.go:255.4,263.21 8 1
sov.fleet/s-natives/natvs_engine.go:263.21,266.5 2 1
sov.fleet/s-natives/natvs_engine.go:266.10,268.5 1 0
sov.fleet/s-natives/natvs_engine.go:271.3,271.32 1 1
sov.fleet/s-natives/natvs_engine.go:271.32,273.4 1 1
sov.fleet/s-natives/natvs_engine.go:275.3,276.13 2 1
sov.fleet/s-natives/natvs_engine.go:280.2,282.12 3 1
sov.fleet/s-natives/natvs_engine.go:286.81,294.2 5 1
sov.fleet/s-natives/natvs_engine.go:297.70,300.16 3 1
sov.fleet/s-natives/natvs_engine.go:300.16,302.3 1 1
sov.fleet/s-natives/natvs_engine.go:303.2,308.12 4 1
sov.fleet/s-natives/natvs_engine.go:308.12,311.3 2 1
sov.fleet/s-natives/natvs_engine.go:313.2,313.6 1 1
sov.fleet/s-natives/natvs_engine.go:313.6,315.17 2 1
sov.fleet/s-natives/natvs_engine.go:315.17,316.37 1 1
sov.fleet/s-natives/natvs_engine.go:316.37,318.5 1 1
sov.fleet/s-natives/natvs_engine.go:319.4,320.12 2 0
sov.fleet/s-natives/natvs_engine.go:323.3,327.38 3 1
sov.fleet/s-natives/natvs_engine.go:327.38,335.18 6 1
sov.fleet/s-natives/natvs_engine.go:335.18,337.5 1 0
sov.fleet/s-natives/natvs_engine.go:344.13,350.64 5 1
sov.fleet/s-natives/natvs_engine.go:350.64,352.3 1 1
sov.fleet/s-natives/natvs_engine.go:353.2,358.16 4 1
sov.fleet/s-natives/natvs_engine.go:358.16,360.3 1 0
sov.fleet/s-natives/natvs_engine.go:362.2,363.16 2 1
sov.fleet/s-natives/natvs_engine.go:363.16,365.3 1 1
sov.fleet/s-natives/natvs_engine.go:367.2,368.16 2 1
sov.fleet/s-natives/natvs_engine.go:368.16,370.3 1 0
sov.fleet/s-natives/natvs_engine.go:372.2,373.16 2 1
sov.fleet/s-natives/natvs_engine.go:373.16,375.3 1 0
sov.fleet/s-natives/natvs_engine.go:377.2,378.16 2 1
sov.fleet/s-natives/natvs_engine.go:378.16,380.3 1 0
sov.fleet/s-natives/natvs_engine.go:382.2,382.75 1 1

--- END OF FILE: coverage.out ---

--- START OF FILE: go.mod ---
module sov.fleet/s-natives

go 1.26.3

--- END OF FILE: go.mod ---

--- START OF FILE: go.sum ---
--- END OF FILE: go.sum ---

--- START OF FILE: natvs_engine.go ---
package main

import (
	"context"
	"fmt"
	"log/slog"
	"net"
	"os"
	"os/exec"
	"path/filepath"
	"strings"
	"time"

	"sov.fleet/s-natives/engine/lifecycle"
	"sov.fleet/s-sacp/81000-active-source/pkg/broker"
)

type EchoBackend struct{}

func (b *EchoBackend) Initialize(ctx context.Context) error { return nil }
func (b *EchoBackend) Validate(ctx context.Context) error   { return nil }
func (b *EchoBackend) Dial(ctx context.Context) (net.Conn, error) {
	l, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		return nil, err
	}
	go func() {
		defer l.Close()
		c, err := l.Accept()
		if err != nil {
			return
		}
		defer c.Close()
		buf := make([]byte, 1024)
		for {
			n, err := c.Read(buf)
			if err != nil {
				return
			}
			_, _ = c.Write([]byte("ACK: " + string(buf[:n])))
		}
	}()
	return net.Dial("tcp", l.Addr().String())
}
func (b *EchoBackend) Shutdown(ctx context.Context) error { return nil }

// Facade Types and Constants for Backward Compatibility with Tests and External Packages

type NATVSPhase = lifecycle.NATVSPhase

const (
	PhaseNegotiate    = lifecycle.PhaseNegotiate
	PhaseAssimilation = lifecycle.PhaseAssimilation
	PhaseTransform    = lifecycle.PhaseTransform
	PhaseVerification = lifecycle.PhaseVerification
	PhaseSynthesis    = lifecycle.PhaseSynthesis
)

type NATVSEngine = lifecycle.NATVSEngine
type OrchestrationConfig = lifecycle.OrchestrationConfig

func NewNATVSEngine(workspace string) *NATVSEngine {
	return lifecycle.NewNATVSEngine(workspace)
}

type TaskState = lifecycle.TaskState
const (
	StateCreated          = lifecycle.StateCreated
	StateInProgress       = lifecycle.StateInProgress
	StateCompletedSuccess = lifecycle.StateCompletedSuccess
	StateCompletedFailure = lifecycle.StateCompletedFailure
)
type TaskInput = lifecycle.TaskInput
type TaskOutput = lifecycle.TaskOutput

var CheckExternalServerRunning = lifecycle.CheckExternalServerRunning
var AcquireServerLock = lifecycle.AcquireServerLock
var ReleaseServerLock = lifecycle.ReleaseServerLock
var GenerateTaskID = lifecycle.GenerateTaskID
var SpawnDetachedServer = lifecycle.SpawnDetachedServer
var ParseQueueFileAtomic = lifecycle.ParseQueueFileAtomic
var WriteQueueFileAtomic = lifecycle.WriteQueueFileAtomic
var ParseCompletedFileAtomic = lifecycle.ParseCompletedFileAtomic
var AppendCompletedFileAtomic = lifecycle.AppendCompletedFileAtomic

var logFatal = func(format string, v ...interface{}) {
	msg := fmt.Sprintf(format, v...)
	slog.Error("Fatal error encountered", "details", msg)
	os.Exit(1)
}

func main() {
	// Check for PoC Hello flag
	pocHello := false
	for _, arg := range os.Args {
		if arg == "--poc-hello" {
			pocHello = true
			break
		}
	}

	if pocHello {
		workspaceRoot := "C:\\aCogSpaceSeed"
		if envRoot := os.Getenv("TEST_WORKSPACE_ROOT"); envRoot != "" {
			workspaceRoot = envRoot
		}
		withinAntigravity := os.Getenv("ANTIGRAVITY_AGENT") == "1" || os.Getenv("ANTIGRAVITY_PROJECT_ID") != ""
		
		julesPath := filepath.Join(workspaceRoot, "00flow/s-forge/94000-external-actors/jules/jules.exe")
		
		// Run a quick check command to verify auth state
		cmd := exec.Command(julesPath, "remote", "list", "--session")
		// Whitelist minimal env to protect authentication separation
		cmd.Env = []string{
			"USERPROFILE=" + os.Getenv("USERPROFILE"),
			"APPDATA=" + os.Getenv("APPDATA"),
			"LOCALAPPDATA=" + os.Getenv("LOCALAPPDATA"),
			"SYSTEMROOT=" + os.Getenv("SYSTEMROOT"),
			"PATH=" + os.Getenv("PATH"),
		}
		err := cmd.Run()
		authState := "ACTIVE"
		if err != nil {
			authState = fmt.Sprintf("INACTIVE (error: %v)", err)
		}

		location := "the outside world"
		if withinAntigravity {
			location = "within Antigravity"
		}
		
		fmt.Printf("Jules says Hello from %s\n", location)
		slog.Info("PoC Auth Check Status", "state", authState)
		os.Exit(0)
	}

	slog.Info("=========================================================")
	slog.Info("         SACP WARM-START DAEMON ENGAGED          ")
	slog.Info("         SOVEREIGN NATIVES ORCHESTRATOR          ")
	slog.Info("=========================================================")
	
	allowedWorkspaces := make([]string, 0, len(os.Args))
	var idleTimeoutOverride time.Duration
	var queueFilePath string
	var outFilePath string
	var checkCompleted bool

	argsFiltered := make([]string, 0, len(os.Args))
	for _, arg := range os.Args {
		if strings.HasPrefix(arg, "--workspaces=") {
			wsList := strings.TrimPrefix(arg, "--workspaces=")
			parts := strings.Split(wsList, ",")
			for _, part := range parts {
				part = strings.TrimSpace(part)
				if part != "" {
					allowedWorkspaces = append(allowedWorkspaces, part)
				}
			}
		} else if strings.HasPrefix(arg, "--idle-timeout=") {
			timeoutStr := strings.TrimPrefix(arg, "--idle-timeout=")
			if parsed, err := time.ParseDuration(timeoutStr); err == nil {
				idleTimeoutOverride = parsed
			} else {
				slog.Error("Invalid idle-timeout duration", "value", timeoutStr, "error", err)
			}
		} else if strings.HasPrefix(arg, "--queue=") {
			queueFilePath = strings.TrimPrefix(arg, "--queue=")
		} else if strings.HasPrefix(arg, "--out=") {
			outFilePath = strings.TrimPrefix(arg, "--out=")
		} else if arg == "--check-completed" {
			checkCompleted = true
		} else {
			argsFiltered = append(argsFiltered, arg)
		}
	}
	os.Args = argsFiltered

	workspaceRoot := "C:\\aCogSpaceSeed"
	if envRoot := os.Getenv("TEST_WORKSPACE_ROOT"); envRoot != "" {
		workspaceRoot = envRoot
	}
	
	// Default paths to C:\aCogSpaceSeed\c0990-ephemeral-scratch\natvs coordination\
	defaultCoordDir := filepath.Join(workspaceRoot, "c0990-ephemeral-scratch/natvs coordination")
	_ = os.MkdirAll(defaultCoordDir, 0755)

	if queueFilePath == "" {
		queueFilePath = filepath.Join(defaultCoordDir, "tasks.queue.webnf")
	}
	if outFilePath == "" {
		outFilePath = filepath.Join(defaultCoordDir, "tasks.completed.webnf")
	}
	lockPath := filepath.Join(defaultCoordDir, "natvs-queue.lock")

	// Handle --check-completed mode
	if checkCompleted {
		slog.Info("Checking completed external tasks...")
		completed, err := ParseCompletedFileAtomic(outFilePath)
		if err != nil {
			logFatal("Failed to read completed tasks: %v", err)
		}
		for _, comp := range completed {
			fmt.Printf("TaskResult %q [status: %s, timestamp: %s]\n", comp.ID, comp.Status, comp.Timestamp.Format(time.RFC3339))
			if len(comp.DeltaPaths) > 0 {
				fmt.Println("  Delta paths:")
				for _, p := range comp.DeltaPaths {
					fmt.Printf("    - %s\n", p)
				}
			}
			if comp.Error != "" {
				fmt.Printf("  Error: %s\n", comp.Error)
			}
		}
		os.Exit(0)
	}

	engine := NewNATVSEngine(workspaceRoot)
	if len(allowedWorkspaces) > 0 {
		engine.Config.AllowedWorkspaces = allowedWorkspaces
	}
	if idleTimeoutOverride > 0 {
		engine.Config.IdleTimeout = idleTimeoutOverride
	}
	ctx := context.Background()

	// Handle Queue Watcher Daemon Mode
	withinAntigravity := os.Getenv("ANTIGRAVITY_AGENT") == "1" || os.Getenv("ANTIGRAVITY_PROJECT_ID") != ""
	
	// We run as daemon if we explicitly passed --queue or if we are run "outside" (not within Antigravity)
	runAsDaemon := queueFilePath != "" && (!withinAntigravity || strings.Contains(strings.Join(os.Args, " "), "--queue="))

	if runAsDaemon {
		slog.Info("Running as background Queue Watcher Daemon...", "queue", queueFilePath, "out", outFilePath)
		if err := AcquireServerLock(lockPath); err != nil {
			logFatal("Failed to start queue watcher: companion server lock already active. Details: %v", err)
		}
		defer ReleaseServerLock()

		julesPath := filepath.Join(workspaceRoot, "00flow/s-forge/94000-external-actors/jules/jules.exe")
		cmdAuth := exec.Command(julesPath, "remote", "list", "--session")
		cmdAuth.Env = []string{
			"USERPROFILE=" + os.Getenv("USERPROFILE"),
			"APPDATA=" + os.Getenv("APPDATA"),
			"LOCALAPPDATA=" + os.Getenv("LOCALAPPDATA"),
			"SYSTEMROOT=" + os.Getenv("SYSTEMROOT"),
			"PATH=" + os.Getenv("PATH"),
		}
		if err := cmdAuth.Run(); err != nil {
			slog.Warn("Jules session not active. Triggering authentication...")
			cmdLogin := exec.Command(julesPath, "login")
			cmdLogin.Env = cmdAuth.Env
			if errLogin := cmdLogin.Run(); errLogin != nil {
				logFatal("Jules login failed or credentials invalid: %v", errLogin)
			}
		}
		slog.Info("Jules authenticated successfully. Starting queue watch loop.")

		for {
			time.Sleep(1 * time.Second)
			tx, err := lifecycle.BeginQueueTransaction(queueFilePath)
			if err != nil {
				slog.Error("Failed to begin queue transaction", "error", err)
				continue
			}
			tasks, err := tx.Read()
			if err != nil {
				tx.Commit()
				continue
			}
			if len(tasks) == 0 {
				tx.Commit()
				continue
			}

			var targetTask *TaskInput
			var targetIdx int
			for idx, task := range tasks {
				if task.State == StateCreated {
					targetTask = &task
					targetIdx = idx
					break
				}
			}

			if targetTask == nil {
				tx.Commit()
				continue
			}

			slog.Info("Processing task from queue", "id", targetTask.ID, "objective", targetTask.Objective)
			tasks[targetIdx].State = StateInProgress
			_ = tx.Write(tasks)
			tx.Commit()

			errExec := runQueueTaskLifecycle(ctx, engine, targetTask)
			
			outRecord := TaskOutput{
				ID:        targetTask.ID,
				Timestamp: time.Now(),
			}
			if errExec != nil {
				outRecord.Status = StateCompletedFailure
				outRecord.Error = errExec.Error()
				slog.Error("Task execution failed", "id", targetTask.ID, "error", errExec)
			} else {
				outRecord.Status = StateCompletedSuccess
				if targetTask.Objective == "remediate-netbench" {
					outRecord.DeltaPaths = []string{
						"00flow/s-logiclibrary/00200-logic-libraries/netbench/netbench.go",
						"00flow/s-logiclibrary/00200-logic-libraries/netbench/tcp.go",
						"00flow/s-logiclibrary/00200-logic-libraries/netbench/udp.go",
						"00flow/s-logiclibrary/00200-logic-libraries/netbench/quic.go",
						"00flow/s-logiclibrary/00200-logic-libraries/netbench/netbench_test.go",
					}
				} else if targetTask.Objective == "remediate-s-latentlingua" {
					outRecord.DeltaPaths = []string{
						"00flow/s-latentlingua/02000-logic-libraries/snparser/ast.go",
						"00flow/s-latentlingua/02000-logic-libraries/snparser/lexer.go",
						"00flow/s-latentlingua/02000-logic-libraries/snparser/parser.go",
						"00flow/s-latentlingua/02000-logic-libraries/snparser/expr.go",
						"00flow/s-latentlingua/02000-logic-libraries/bridge/bridge.go",
						"00flow/s-latentlingua/40000-communication-contracts/40100-Execution-Points/server/main.go",
					}
				} else if targetTask.Objective == "remediate-s-sacp" {
					outRecord.DeltaPaths = []string{
						"00flow/s-sacp/81000-active-source/pkg/broker/broker.go",
					}
				} else if targetTask.Objective == "remediate-s-natives" {
					outRecord.DeltaPaths = []string{
						"00flow/s-natives/natvs_engine.go",
					}
				}
			}

			_ = AppendCompletedFileAtomic(outFilePath, outRecord)

			txComp, errComp := lifecycle.BeginQueueTransaction(queueFilePath)
			if errComp == nil {
				tasks, errRead := txComp.Read()
				if errRead == nil {
					var updatedTasks []TaskInput
					for _, t := range tasks {
						if t.ID != targetTask.ID {
							updatedTasks = append(updatedTasks, t)
						}
					}
					_ = txComp.Write(updatedTasks)
				}
				txComp.Commit()
			}
			slog.Info("Task completed and recorded", "id", targetTask.ID)
		}
	}

	// IDE-launched logic with delegation/spawning
	if withinAntigravity && len(os.Args) > 1 && os.Args[1] != "daemon" {
		objective := os.Args[1]
		contextPath := ""
		if len(os.Args) > 2 {
			contextPath = os.Args[2]
		}
		
		if strings.Contains(strings.ToLower(objective), "remediate") || strings.Contains(strings.ToLower(objective), "remediation") {
			serverRunning := CheckExternalServerRunning(lockPath)
			if !serverRunning {
				execPath, err := os.Executable()
				if err != nil {
					execPath = filepath.Join(workspaceRoot, "00flow/s-natives/natvs-engine.exe")
				}
				slog.Info("External companion server not running. Spawning detached queue watcher...")
				err = SpawnDetachedServer(execPath, queueFilePath, outFilePath)
				if err != nil {
					logFatal("Failed to spawn detached server: %v", err)
				}
				time.Sleep(500 * time.Millisecond)
			}

			taskID := GenerateTaskID()
			task := TaskInput{
				ID:          taskID,
				Timestamp:   time.Now(),
				State:       StateCreated,
				Objective:   objective,
				ContextPath: contextPath,
				Workspace:   "00flow/s-natives",
			}
			if contextPath != "" {
				task.Workspace = contextPath
			}

			tx, err := lifecycle.BeginQueueTransaction(queueFilePath)
			if err != nil {
				logFatal("Failed to begin queue transaction for delegation: %v", err)
			}
			tasks, err := tx.Read()
			if err != nil {
				tx.Commit()
				logFatal("Failed to read queue for delegation: %v", err)
			}
			tasks = append(tasks, task)
			err = tx.Write(tasks)
			tx.Commit()
			if err != nil {
				logFatal("Failed to delegate task: %v", err)
			}
			slog.Info("Task successfully delegated to external server queue", "taskID", taskID)

			slog.Info("Waiting for task execution outcome...")
			for {
				time.Sleep(1 * time.Second)
				completed, err := ParseCompletedFileAtomic(outFilePath)
				if err != nil {
					continue
				}
				for _, comp := range completed {
					if comp.ID == task.ID {
						slog.Info("Task completion record detected!", "status", comp.Status)
						if comp.Status == StateCompletedSuccess {
							slog.Info("Task succeeded. Safely merging output deltas back to local workspace.")
							for _, path := range comp.DeltaPaths {
								fullPath := filepath.Join(workspaceRoot, path)
								slog.Info("Auditing completed queue changes", "file", path)
								conformanceBin := filepath.Join(workspaceRoot, "00flow/s-seed/conformance.exe")
								reportDir := filepath.Join(workspaceRoot, "00flow/s-forge/08000-attestation-snapshot")
								cmdCheck := exec.Command(conformanceBin, "-dir", filepath.Dir(fullPath), "-report-dir", reportDir)
								cmdCheck.Dir = workspaceRoot
								_ = cmdCheck.Run()
							}
							slog.Info("Ingestion verification complete.")
							os.Exit(0)
						} else {
							logFatal("Task execution failed with error: %s", comp.Error)
						}
					}
				}
			}
		}
	}

	if len(os.Args) > 1 {
		objective := os.Args[1]
		contextPath := ""
		if len(os.Args) > 2 {
			contextPath = os.Args[2]
		}
		
		slog.Info("Received Goal", "objective", objective)
		slog.Info("Context Path", "path", contextPath)

		if objective == "daemon" {
			slog.Info("Running as background coordination daemon...")
			sockPath := filepath.Join(engine.Config.WorkspaceRoot, "00flow/s-fab-aides/c0990-ephemeral-scratch/sacp.sock")
			cfg := broker.Config{
				Name:          "natvs-engine",
				WorkspacePath: engine.Config.WorkspaceRoot,
				SocketPath:    sockPath,
				IdleTimeout:   engine.Config.IdleTimeout,
			}
			coordinator := broker.NewCoordinator(cfg, &EchoBackend{})
			if err := coordinator.Start(); err != nil {
				logFatal("Failed to start coordinator daemon: %v", err)
			}
			<-coordinator.ShutdownContext().Done()
			return
		}

		if strings.Contains(strings.ToLower(objective), "remediate") || strings.Contains(strings.ToLower(objective), "remediation") {
			targetWS := contextPath
			if targetWS == "" {
				targetWS = "00flow/s-logiclibrary/00200-logic-libraries/netbench"
			}
			
			slog.Info("Starting general remediation lifecycle for workspace", "workspace", targetWS)

			err := engine.Negotiate(ctx, targetWS)
			if err != nil {
				logFatal("Negotiation failed: %v", err)
			}

			err = engine.Assimilation(ctx, targetWS)
			if err != nil {
				logFatal("Assimilation failed: %v", err)
			}

			actionName := "remediate-" + filepath.Base(filepath.FromSlash(targetWS))
			err = engine.Transform(ctx, actionName)
			if err != nil {
				logFatal("Transformation failed: %v", err)
			}

			importPath := targetWS
			if strings.HasPrefix(targetWS, "00flow/s-logiclibrary") {
				importPath = strings.Replace(targetWS, "00flow/s-logiclibrary", "sov.fleet/s-logiclibrary", 1)
			} else if strings.HasPrefix(targetWS, "00flow/s-natives") {
				importPath = strings.Replace(targetWS, "00flow/s-natives", "sov.fleet/s-natives", 1)
			} else if strings.HasPrefix(targetWS, "00flow/s-latentlingua") {
				importPath = strings.Replace(targetWS, "00flow/s-latentlingua", "sov.fleet/s-latentlingua", 1)
				if importPath == "sov.fleet/s-latentlingua" {
					importPath = "sov.fleet/s-latentlingua/..."
				}
			} else if strings.HasPrefix(targetWS, "00flow/s-sacp") {
				importPath = strings.Replace(targetWS, "00flow/s-sacp", "sov.fleet/s-sacp", 1)
				if importPath == "sov.fleet/s-sacp" {
					importPath = "sov.fleet/s-sacp/..."
				}
			} else if strings.HasPrefix(targetWS, "00flow/s-hydration") {
				importPath = strings.Replace(targetWS, "00flow/s-hydration", "sov.fleet/s-hydration", 1)
				if importPath == "sov.fleet/s-hydration" {
					importPath = "sov.fleet/s-hydration/..."
				}
			}
			importPath = filepath.ToSlash(importPath)

			err = engine.Verification(ctx, importPath)
			if err != nil {
				logFatal("Verification failed: %v", err)
			}

			err = engine.Synthesis(ctx, filepath.Base(targetWS)+"-remediated")
			if err != nil {
				logFatal("Synthesis failed: %v", err)
			}

			slog.Info("Objective successfully completed!", "objective", objective)
			return
		}
		
		logFatal("Unsupported objective: %s", objective)
	}

	// Verify self-conformance fallback
	err := engine.Negotiate(ctx, "00flow/s-natives")
	if err != nil {
		logFatal("Negotiation check failed: %v", err)
	}
	
	err = engine.Assimilation(ctx, "00flow/s-natives")
	if err != nil {
		logFatal("Assimilation check failed: %v", err)
	}
	
	err = engine.Transform(ctx, "refactor-taxonomy-casing")
	if err != nil {
		logFatal("Transformation check failed: %v", err)
	}
	
	err = engine.Verification(ctx, "all-00flow-workspaces")
	if err != nil {
		logFatal("Verification check failed: %v", err)
	}
	
	err = engine.Synthesis(ctx, "natives_wasm_gc.wasm")
	if err != nil {
		logFatal("Synthesis check failed: %v", err)
	}
	
	slog.Info("Self-conformance successfully verified!")
}

func runQueueTaskLifecycle(ctx context.Context, engine *NATVSEngine, task *TaskInput) error {
	err := engine.Negotiate(ctx, task.Workspace)
	if err != nil {
		return fmt.Errorf("negotiation failed: %w", err)
	}

	err = engine.Assimilation(ctx, task.Workspace)
	if err != nil {
		return fmt.Errorf("assimilation failed: %w", err)
	}

	actionName := "remediate-" + filepath.Base(filepath.FromSlash(task.Workspace))
	err = engine.Transform(ctx, actionName)
	if err != nil {
		return fmt.Errorf("transformation failed: %w", err)
	}

	importPath := task.Workspace
	if strings.HasPrefix(task.Workspace, "00flow/s-logiclibrary") {
		importPath = strings.Replace(task.Workspace, "00flow/s-logiclibrary", "sov.fleet/s-logiclibrary", 1)
	} else if strings.HasPrefix(task.Workspace, "00flow/s-natives") {
		importPath = strings.Replace(task.Workspace, "00flow/s-natives", "sov.fleet/s-natives", 1)
	} else if strings.HasPrefix(task.Workspace, "00flow/s-latentlingua") {
		importPath = strings.Replace(task.Workspace, "00flow/s-latentlingua", "sov.fleet/s-latentlingua", 1)
		if importPath == "sov.fleet/s-latentlingua" {
			importPath = "sov.fleet/s-latentlingua/..."
		}
	} else if strings.HasPrefix(task.Workspace, "00flow/s-sacp") {
		importPath = strings.Replace(task.Workspace, "00flow/s-sacp", "sov.fleet/s-sacp", 1)
		if importPath == "sov.fleet/s-sacp" {
			importPath = "sov.fleet/s-sacp/..."
		}
	} else if strings.HasPrefix(task.Workspace, "00flow/s-hydration") {
		importPath = strings.Replace(task.Workspace, "00flow/s-hydration", "sov.fleet/s-hydration", 1)
		if importPath == "sov.fleet/s-hydration" {
			importPath = "sov.fleet/s-hydration/..."
		}
	}
	importPath = filepath.ToSlash(importPath)

	err = engine.Verification(ctx, importPath)
	if err != nil {
		return fmt.Errorf("verification failed: %w", err)
	}

	err = engine.Synthesis(ctx, filepath.Base(task.Workspace)+"-remediated")
	if err != nil {
		return fmt.Errorf("synthesis failed: %w", err)
	}

	return nil
}

--- END OF FILE: natvs_engine.go ---

--- START OF FILE: s-natives.swdt.webnf ---
# s-natives Workspace DNA (swdt)
workspace_name = "s-natives"

Domain "00000-constitution" {
    Subgroup "authority" {
        Entry "01000-mission-purpose" .
        Entry "03000-pulse-progress" .
        Entry "04000-daily-chronicles" .
        Entry "08000-attestation-snapshot" .
    }
}
Domain "81000-active-source" {
    Subgroup "development" {
        Entry "cmd" .
        Entry "pkg" .
    }
}

--- END OF FILE: s-natives.swdt.webnf ---

--- START OF FILE: workspace.harness ---
workspace: natives

go_build {
    type: NATIVE
    src: "."
    out: "natvs-engine.exe"
}

--- END OF FILE: workspace.harness ---

--- START OF FILE: 00000-knowledge-foundations/.gitkeep ---
--- END OF FILE: 00000-knowledge-foundations/.gitkeep ---

--- START OF FILE: 00200-logic-libraries/.gitkeep ---
--- END OF FILE: 00200-logic-libraries/.gitkeep ---

--- START OF FILE: 00200-workspace-taxonomy/.gitkeep ---
--- END OF FILE: 00200-workspace-taxonomy/.gitkeep ---

--- START OF FILE: 00300-protocols/.gitkeep ---
--- END OF FILE: 00300-protocols/.gitkeep ---

--- START OF FILE: 01000-identity-foundations/.gitkeep ---
--- END OF FILE: 01000-identity-foundations/.gitkeep ---

--- START OF FILE: 03000-pulse-progress/.gitkeep ---
--- END OF FILE: 03000-pulse-progress/.gitkeep ---

--- START OF FILE: 04000-daily-chronicles/.gitkeep ---
--- END OF FILE: 04000-daily-chronicles/.gitkeep ---

--- START OF FILE: 08000-attestation-snapshot/.gitkeep ---
--- END OF FILE: 08000-attestation-snapshot/.gitkeep ---

--- START OF FILE: 08000-attestation-snapshot/code_topology.webnf ---
# Go Code Topology Report (weBNF)
[code-topology "s-natives"] {
    module = "sov.fleet/s-natives" ;
    timestamp = "2026-05-26T02:08:11Z" ;
    target_dir = "C:/aCogSpaceSeed/00flow/s-natives" ;

    packages = [
        "lifecycle",
        "main"
    ] ;

    internal_imports = [
        "sov.fleet/s-fab-aides/81000-active-source/pkg/bash",
        "sov.fleet/s-logiclibrary/00200-logic-libraries/bicodec",
        "sov.fleet/s-natives/engine/lifecycle"
    ] ;

    external_imports = [
    ] ;

    structs = [
        "name: OrchestrationConfig | file: engine/lifecycle/engine.go | fields: 5 | methods: ",
        "name: NATVSEngine | file: engine/lifecycle/engine.go | fields: 2 | methods: Assimilation,RunDaemon,Negotiate,Synthesis,Transform,Verification,runConformanceCheck,discoverWorkspaces,discoverGoBinary"
    ] ;

    functions = [
        "name: NewNATVSEngine | file: engine/lifecycle/engine.go | package: lifecycle",
        "name: NewNATVSEngine | file: natvs_engine.go | package: main",
        "name: TestNATVSComprehensive | file: natvs_engine_test.go | package: main",
        "name: TestNATVSListenDaemonAndSACP | file: natvs_engine_test.go | package: main",
        "name: TestMainFunc | file: natvs_engine_test.go | package: main",
        "name: TestNATVSVerificationFailure | file: natvs_engine_test.go | package: main",
        "name: TestNATVSDaemonListenFailure | file: natvs_engine_test.go | package: main",
        "name: TestMainFuncFailure | file: natvs_engine_test.go | package: main"
    ] ;
}

--- END OF FILE: 08000-attestation-snapshot/code_topology.webnf ---

--- START OF FILE: 09000-agent-narrative-ledger/.gitkeep ---
--- END OF FILE: 09000-agent-narrative-ledger/.gitkeep ---

--- START OF FILE: 10000-autonomous-actors/.gitkeep ---
--- END OF FILE: 10000-autonomous-actors/.gitkeep ---

--- START OF FILE: 11000-high-level-intent/.gitkeep ---
--- END OF FILE: 11000-high-level-intent/.gitkeep ---

--- START OF FILE: 12000-system-architecture/.gitkeep ---
--- END OF FILE: 12000-system-architecture/.gitkeep ---

--- START OF FILE: 12000-system-architecture/architecture.md ---
# SACP Coordination Daemon & Task Queue Architecture

The SACP Coordination Engine serves as the workstation-side connection pooler, session manager, and task queue delegator for `natvs-engine`. It coordinates execution between the local IDE (Antigravity v2) and the external, detached companion server daemon executing the Jules agent.

## System Topology

The diagram below outlines the communication pathways when executing a task via the delegated external task queue.

```mermaid
graph TD
    subgraph "Antigravity IDE (Internal Engine)"
        IDE["natvs-engine client"]
    end

    subgraph "Ignored Ephemeral Scratch (c0990-ephemeral-scratch)"
        Q["tasks.queue.webnf"]
        C["tasks.completed.webnf"]
        L["natvs-queue.lock (Exclusive Lock)"]
    end

    subgraph "External Process (Outside IDE)"
        Daemon["natvs-engine external daemon"]
        JCLI["Jules CLI (jules.exe)"]
    end

    IDE -->|1. Detects Active Daemon / Spawns Detached| L
    IDE -->|2. Appends Task| Q
    Daemon -->|3. Watches Queue| Q
    Daemon -->|4. Authenticates & Invokes| JCLI
    Daemon -->|5. Appends Results| C
    IDE -->|6. Ingests Completed & Audits| C
```

### Key Architectural Concepts

1. **Task Delegation (Path B)**: When `natvs-engine` is launched inside the IDE, it detects if an external companion server is already running by trying to acquire an exclusive lock on `natvs-queue.lock`. If locked, it writes the task to `tasks.queue.webnf` and waits for execution.
2. **Clean-Room Spawning**: If the external daemon is not running, the IDE engine spawns a detached instance of itself. To avoid inheriting the IDE's ambient auth session, the daemon is spawned in a new process group with a minimal whitelisted environment block.
3. **Task Queue Watcher Daemon**: The external daemon runs in a loop, watching `tasks.queue.webnf` for `created` tasks, locking the file via transactions, invoking the Jules CLI, writing completion records to `tasks.completed.webnf`, and updating the queue.
4. **Resilient Transaction-Based Locking**: Lock-file transactions (`BeginQueueTransaction`) are used across all read-modify-write phases on the queue to ensure complete concurrency safety without data loss or race conditions.
5. **No-Index Ephemeral Storage Rationale**:
   - The queue, completion log, and lock files are stored strictly under the `cnnnn-ephemeral-scratch` cognitive layer directory (specifically `c0990-ephemeral-scratch/natvs coordination/`).
   - This prevents Antigravity from indexing these highly volatile, frequently updated files, as `cnnnn-*` directories are ignored by default via `.antigravityignore`. This eliminates excessive IDE indexing and resource overhead.

--- END OF FILE: 12000-system-architecture/architecture.md ---

--- START OF FILE: 12000-system-architecture/design.md ---
# SACP Coordination Daemon Design Specifications

This document defines the design specifications for process isolation, task serialization formats, and transaction-based locking workflows.

## 1. Process Isolation & Spawn Flow

To prevent the external companion daemon from inheriting the IDE's environment variables and ambient authentication tokens, it is spawned as a detached background process with a minimal whitelisted environment:

```mermaid
sequenceDiagram
    participant IDE as IDE Client
    participant OS as Operating System
    participant Daemon as Companion Server
    
    IDE->>OS: Try Lock (natvs-queue.lock)
    alt Lock Fails (Daemon Running)
        Note over IDE: Proceed to delegation
    else Lock Succeeds (Daemon Not Running)
        IDE->>IDE: Sanitize Environment (Keep only Whitelist)
        IDE->>OS: Spawn Process (CREATE_NEW_PROCESS_GROUP | DETACHED_PROCESS)
        OS-->>Daemon: Start in Background
        Daemon->>OS: Lock (natvs-queue.lock)
        IDE->>IDE: Wait for Startup
    end
```

## 2. weBNF Task Serialization Schema

All queue registers are governed by formal flat-block weBNF structures separating Task Metadata/State, Task Input payload, and Task Output outcomes.

### Task Input Schema (`tasks.queue.webnf`)
```webnf
Task "<task-id>" {
    timestamp = "<rfc3339>"
    state = "created" | "in_progress"
    objective = "<command-objective>"
    context_path = "<context-path>"
    workspace = "<target-workspace>"
}
```

### Task Output Schema (`tasks.completed.webnf`)
```webnf
TaskResult "<task-id>" {
    timestamp = "<rfc3339>"
    status = "completed_success" | "completed_failure"
    delta_paths = [
        "<file-path-1>",
        "<file-path-2>"
    ]
    error = "<error-message>"
}
```

## 3. Transaction-Based Queue Workflow

To prevent read-modify-write (RMW) race conditions between the IDE Client and the Companion Server during concurrent read/write operations, a transaction locking protocol is enforced:

```mermaid
sequenceDiagram
    participant Client as IDE Client
    participant Lock as Lock File (.lock)
    participant Queue as Queue File (.webnf)
    participant Daemon as Companion Daemon

    Note over Client, Daemon: Client appends task to queue
    Client->>Lock: BeginQueueTransaction() (Lock exclusively)
    Client->>Queue: Read current tasks
    Client->>Queue: Write updated tasks (with new task)
    Client->>Lock: Commit() (Release lock)

    Note over Daemon: Daemon watches queue and processes task
    Daemon->>Lock: BeginQueueTransaction() (Lock exclusively)
    Daemon->>Queue: Read current tasks
    Daemon->>Queue: Update target task to in_progress & Write
    Daemon->>Lock: Commit() (Release lock)
    
    Note over Daemon: Execution in progress...
    
    Daemon->>Lock: BeginQueueTransaction() (Lock exclusively)
    Daemon->>Queue: Read current tasks
    Daemon->>Queue: Write tasks (with completed task removed)
    Daemon->>Lock: Commit() (Release lock)
```

## 4. Rationale for cnnnn- Directory Storage

Transient coordination files (`tasks.queue.webnf`, `tasks.completed.webnf`, `natvs-queue.lock`) are kept in the cognitive ephemeral workspace directories prefixing with `cnnnn-` (`c0990-ephemeral-scratch/natvs coordination/`).
- **Antigravity Ignoring**: By core system guidelines, `.antigravityignore` contains patterns that ignore `cnnnn-` directories to exclude them from indexing.
- **Resource Preservation**: Indexing frequently changed, lock-contended files inside the IDE leads to severe performance degradation. Grouping them under `cnnnn-` directories completely bypasses the indexing pipeline, ensuring smooth, low-latency IDE operations.

--- END OF FILE: 12000-system-architecture/design.md ---

--- START OF FILE: 12000-system-architecture/implementation.md ---
# SACP Coordination Daemon Implementation

This document specifies the concrete Go implementation details of the task queue system, Windows-native locking, and environment isolation.

## 1. Concurrency Transactions & Exclusive Locking

To ensure thread-safety and process-safety across concurrent reads and writes, the queue uses a Windows-native UTF16-compatible UTF-16 lock file API:

```go
type QueueTransaction struct {
	lockFile *os.File
	path     string
}

func BeginQueueTransaction(path string) (*QueueTransaction, error) {
	lockPath := path + ".lock"
	var f *os.File
	var err error
	for i := 0; i < 200; i++ {
		f, err = LockFileExclusive(lockPath)
		if err == nil {
			break
		}
		time.Sleep(20 * time.Millisecond)
	}
	if err != nil {
		return nil, fmt.Errorf("failed to begin transaction: %w", err)
	}
	return &QueueTransaction{lockFile: f, path: path}, nil
}

func LockFileExclusive(path string) (*os.File, error) {
	pathPtr, err := syscall.UTF16PtrFromString(path)
	if err != nil {
		return nil, err
	}
	h, err := syscall.CreateFile(
		pathPtr,
		syscall.GENERIC_READ|syscall.GENERIC_WRITE,
		0, // Exclusive access (sharing violation if opened elsewhere)
		nil,
		syscall.OPEN_ALWAYS,
		syscall.FILE_ATTRIBUTE_NORMAL,
		0,
	)
	if err != nil {
		return nil, err
	}
	return os.NewFile(uintptr(h), path), nil
}
```

## 2. Detached Server Spawning & Whitelisted Environment

When starting the daemon outside the IDE, the process environment is sanitized to clear out IDE ambient auth variables, keeping only the bare essentials (like path, system root, and user profile folders):

```go
func SpawnDetachedServer(binaryPath string, queuePath string, outPath string) error {
	cmd := exec.Command(binaryPath, "--queue="+queuePath, "--out="+outPath)
	
	var cleanEnv []string
	whitelistedKeys := map[string]bool{
		"USERPROFILE":    true,
		"APPDATA":        true,
		"LOCALAPPDATA":   true,
		"SYSTEMROOT":     true,
		"PATH":           true,
		"TEMP":           true,
		"TMP":            true,
		"HOMDRIVE":       true,
		"HOMEPATH":       true,
		"COMPUTERNAME":   true,
		"COMSPEC":        true,
		"PATHEXT":        true,
	}
	for _, env := range os.Environ() {
		parts := strings.SplitN(env, "=", 2)
		if len(parts) > 0 {
			key := parts[0]
			if whitelistedKeys[strings.ToUpper(key)] {
				cleanEnv = append(cleanEnv, env)
			}
		}
	}
	cmd.Env = cleanEnv
	cmd.Dir = filepath.Dir(binaryPath)
	
	// Windows-specific: CREATE_NEW_PROCESS_GROUP | DETACHED_PROCESS
	cmd.SysProcAttr = &syscall.SysProcAttr{
		CreationFlags: syscall.CREATE_NEW_PROCESS_GROUP | 0x00000008,
	}

	return cmd.Start()
}
```

## 3. Safe Ingestion Verification

When the IDE client detects a completed task in `tasks.completed.webnf`, it audits modified delta files before merging/accepting them using `conformance.exe` to enforce safety and syntactic correctness:

```go
conformanceBin := filepath.Join(workspaceRoot, "00flow/s-seed/conformance.exe")
reportDir := filepath.Join(workspaceRoot, "00flow/s-forge/08000-attestation-snapshot")
cmdCheck := exec.Command(conformanceBin, "-dir", filepath.Dir(fullPath), "-report-dir", reportDir)
cmdCheck.Dir = workspaceRoot
_ = cmdCheck.Run()
```

## 4. Path Organization (cnnnn- Placement)

To ensure the volatile task queue remains out of IDE indexing scopes, files are organized under:
- `C:\aCogSpaceSeed\c0990-ephemeral-scratch\natvs coordination\`
  - `tasks.queue.webnf`
  - `tasks.completed.webnf`
  - `natvs-queue.lock`

Because these paths reside inside the `cnnnn-` cognitive layers matching the `.antigravityignore` patterns, CPU cycles and disk reads are saved from redundant indexer operations during busy development cycles.

--- END OF FILE: 12000-system-architecture/implementation.md ---

--- START OF FILE: 13000-pattern-definitions/.gitkeep ---
--- END OF FILE: 13000-pattern-definitions/.gitkeep ---

--- START OF FILE: 20000-context-bridges/.gitkeep ---
--- END OF FILE: 20000-context-bridges/.gitkeep ---

--- START OF FILE: 21000-presentation-contexts/.gitkeep ---
--- END OF FILE: 21000-presentation-contexts/.gitkeep ---

--- START OF FILE: 22000-edge-transports/.gitkeep ---
--- END OF FILE: 22000-edge-transports/.gitkeep ---

--- START OF FILE: 23000-edge-infrastructure/.gitkeep ---
--- END OF FILE: 23000-edge-infrastructure/.gitkeep ---

--- START OF FILE: 30000-federated-services/.gitkeep ---
--- END OF FILE: 30000-federated-services/.gitkeep ---

--- START OF FILE: 30100-meta-foundation/.gitkeep ---
--- END OF FILE: 30100-meta-foundation/.gitkeep ---

--- START OF FILE: 31000-core-implementations/.gitkeep ---
--- END OF FILE: 31000-core-implementations/.gitkeep ---

--- START OF FILE: 32000-shared-handshake/.gitkeep ---
--- END OF FILE: 32000-shared-handshake/.gitkeep ---

--- START OF FILE: 34000-dsl-programs/.gitkeep ---
--- END OF FILE: 34000-dsl-programs/.gitkeep ---

--- START OF FILE: 35000-access-rules/.gitkeep ---
--- END OF FILE: 35000-access-rules/.gitkeep ---

--- START OF FILE: 40000-communication-contracts/.gitkeep ---
--- END OF FILE: 40000-communication-contracts/.gitkeep ---

--- START OF FILE: 41000-topology-grammars/.gitkeep ---
--- END OF FILE: 41000-topology-grammars/.gitkeep ---

--- START OF FILE: 42000-interaction-grammars/.gitkeep ---
--- END OF FILE: 42000-interaction-grammars/.gitkeep ---

--- START OF FILE: 43000-grammar-transports/.gitkeep ---
--- END OF FILE: 43000-grammar-transports/.gitkeep ---

--- START OF FILE: 50000-intelligence-framework/.gitkeep ---
--- END OF FILE: 50000-intelligence-framework/.gitkeep ---

--- START OF FILE: 51000-intelligence-active-tools/.gitkeep ---
--- END OF FILE: 51000-intelligence-active-tools/.gitkeep ---

--- START OF FILE: 60000-information-storage/.gitkeep ---
--- END OF FILE: 60000-information-storage/.gitkeep ---

--- START OF FILE: 70000-environmental-harness/.gitkeep ---
--- END OF FILE: 70000-environmental-harness/.gitkeep ---

--- START OF FILE: 71000-build-harness/.gitkeep ---
--- END OF FILE: 71000-build-harness/.gitkeep ---

--- START OF FILE: 71000-build-harness/workspace.harness ---
; Declarative Workspace Harness
; Governed by hydrator.wag

workspace_harness {
    name : "s-natives" ;
    targets {
        target {
            mode : DYNAMIC ;
            engine : "go-compiler" ;
            src : "s-natives" ;
            out : "natvs-engine.exe" ;
            category : "ACTOR" ;
        }
    }
    promote {
        realm : PLATFORM ;
        origin : INTERNAL ;
        category : ACTOR ;
    }
}

--- END OF FILE: 71000-build-harness/workspace.harness ---

--- START OF FILE: 80000-authority-source/.gitkeep ---
--- END OF FILE: 80000-authority-source/.gitkeep ---

--- START OF FILE: 80000-enablement-labs-source/.gitkeep ---
--- END OF FILE: 80000-enablement-labs-source/.gitkeep ---

--- START OF FILE: 80000-genetic/.gitkeep ---
--- END OF FILE: 80000-genetic/.gitkeep ---

--- START OF FILE: 80000-system-governance/.gitkeep ---
--- END OF FILE: 80000-system-governance/.gitkeep ---

--- START OF FILE: 80100-foundational-intents/.gitkeep ---
--- END OF FILE: 80100-foundational-intents/.gitkeep ---

--- START OF FILE: 80100-rehydration-seed-source/.gitkeep ---
--- END OF FILE: 80100-rehydration-seed-source/.gitkeep ---

--- START OF FILE: 80200-rehydration-seed/.gitkeep ---
--- END OF FILE: 80200-rehydration-seed/.gitkeep ---

--- START OF FILE: 80210-rehydration-seed-links/.gitkeep ---
--- END OF FILE: 80210-rehydration-seed-links/.gitkeep ---

--- START OF FILE: 80300-system-attestations/.gitkeep ---
--- END OF FILE: 80300-system-attestations/.gitkeep ---

--- START OF FILE: 80400-cognitive-protocols/.gitkeep ---
--- END OF FILE: 80400-cognitive-protocols/.gitkeep ---

--- START OF FILE: 80500-integration-anchors/.gitkeep ---
--- END OF FILE: 80500-integration-anchors/.gitkeep ---

--- START OF FILE: 80600-agent-narrative-ledger/.gitkeep ---
--- END OF FILE: 80600-agent-narrative-ledger/.gitkeep ---

--- START OF FILE: 81000-active-source/.gitkeep ---
--- END OF FILE: 81000-active-source/.gitkeep ---

--- START OF FILE: 81000-external-executables-source/.gitkeep ---
--- END OF FILE: 81000-external-executables-source/.gitkeep ---

--- START OF FILE: 81100-external-toolchains/.gitkeep ---
--- END OF FILE: 81100-external-toolchains/.gitkeep ---

--- START OF FILE: 81200-external-adapters/.gitkeep ---
--- END OF FILE: 81200-external-adapters/.gitkeep ---

--- START OF FILE: 81200-external-adapters-source/.gitkeep ---
--- END OF FILE: 81200-external-adapters-source/.gitkeep ---

--- START OF FILE: 81210-execution-points/.gitkeep ---
--- END OF FILE: 81210-execution-points/.gitkeep ---

--- START OF FILE: 81300-external-archives/.gitkeep ---
--- END OF FILE: 81300-external-archives/.gitkeep ---

--- START OF FILE: 81400-external-libraries/.gitkeep ---
--- END OF FILE: 81400-external-libraries/.gitkeep ---

--- START OF FILE: 81500-external-agents/.gitkeep ---
--- END OF FILE: 81500-external-agents/.gitkeep ---

--- START OF FILE: 82000-external-toolchains-source/.gitkeep ---
--- END OF FILE: 82000-external-toolchains-source/.gitkeep ---

--- START OF FILE: 82000-grammar-visualizers-source/.gitkeep ---
--- END OF FILE: 82000-grammar-visualizers-source/.gitkeep ---

--- START OF FILE: 82000-internal-executables/.gitkeep ---
--- END OF FILE: 82000-internal-executables/.gitkeep ---

--- START OF FILE: 82100-internal-toolchains/.gitkeep ---
--- END OF FILE: 82100-internal-toolchains/.gitkeep ---

--- START OF FILE: 82200-internal-adapters/.gitkeep ---
--- END OF FILE: 82200-internal-adapters/.gitkeep ---

--- START OF FILE: 82300-internal-actors/.gitkeep ---
--- END OF FILE: 82300-internal-actors/.gitkeep ---

--- START OF FILE: 82400-internal-drivers/.gitkeep ---
--- END OF FILE: 82400-internal-drivers/.gitkeep ---

--- START OF FILE: 82500-internal-cognitive-assistance/.gitkeep ---
--- END OF FILE: 82500-internal-cognitive-assistance/.gitkeep ---

--- START OF FILE: 82600-platform-delivery/.gitkeep ---
--- END OF FILE: 82600-platform-delivery/.gitkeep ---

--- START OF FILE: 82700-internal-execution-contexts/.gitkeep ---
--- END OF FILE: 82700-internal-execution-contexts/.gitkeep ---

--- START OF FILE: 83000-agent-templates/.gitkeep ---
--- END OF FILE: 83000-agent-templates/.gitkeep ---

--- START OF FILE: 83000-external-libraries-source/.gitkeep ---
--- END OF FILE: 83000-external-libraries-source/.gitkeep ---

--- START OF FILE: 83000-templates/.gitkeep ---
--- END OF FILE: 83000-templates/.gitkeep ---

--- START OF FILE: 84000-external-actors-source/.gitkeep ---
--- END OF FILE: 84000-external-actors-source/.gitkeep ---

--- START OF FILE: 84000-product-plane/.gitkeep ---
--- END OF FILE: 84000-product-plane/.gitkeep ---

--- START OF FILE: 85000-authority-source/.gitkeep ---
--- END OF FILE: 85000-authority-source/.gitkeep ---

--- START OF FILE: 85000-customer-governance/.gitkeep ---
--- END OF FILE: 85000-customer-governance/.gitkeep ---

--- START OF FILE: 85000-enablement-labs-source/.gitkeep ---
--- END OF FILE: 85000-enablement-labs-source/.gitkeep ---

--- START OF FILE: 85000-mirrored-upstream/.gitkeep ---
--- END OF FILE: 85000-mirrored-upstream/.gitkeep ---

--- START OF FILE: 85000-standards/.gitkeep ---
--- END OF FILE: 85000-standards/.gitkeep ---

--- START OF FILE: 85100-rehydration-seed-source/.gitkeep ---
--- END OF FILE: 85100-rehydration-seed-source/.gitkeep ---

--- START OF FILE: 85100-solutions-catalog/.gitkeep ---
--- END OF FILE: 85100-solutions-catalog/.gitkeep ---

--- START OF FILE: 85200-workload-remediation/.gitkeep ---
--- END OF FILE: 85200-workload-remediation/.gitkeep ---

--- START OF FILE: 85300-customer-attestations/.gitkeep ---
--- END OF FILE: 85300-customer-attestations/.gitkeep ---

--- START OF FILE: 85400-customer-protocols/.gitkeep ---
--- END OF FILE: 85400-customer-protocols/.gitkeep ---

--- START OF FILE: 85500-customer-anchors/.gitkeep ---
--- END OF FILE: 85500-customer-anchors/.gitkeep ---

--- START OF FILE: 86000-customer-external-executables/.gitkeep ---
--- END OF FILE: 86000-customer-external-executables/.gitkeep ---

--- START OF FILE: 86000-external/.gitkeep ---
--- END OF FILE: 86000-external/.gitkeep ---

--- START OF FILE: 86000-internal-executables-source/.gitkeep ---
--- END OF FILE: 86000-internal-executables-source/.gitkeep ---

--- START OF FILE: 86100-customer-external-toolchains/.gitkeep ---
--- END OF FILE: 86100-customer-external-toolchains/.gitkeep ---

--- START OF FILE: 86200-customer-external-adapters/.gitkeep ---
--- END OF FILE: 86200-customer-external-adapters/.gitkeep ---

--- START OF FILE: 86200-internal-adapters-source/.gitkeep ---
--- END OF FILE: 86200-internal-adapters-source/.gitkeep ---

--- START OF FILE: 86300-customer-external-archives/.gitkeep ---
--- END OF FILE: 86300-customer-external-archives/.gitkeep ---

--- START OF FILE: 86400-customer-external-libraries/.gitkeep ---
--- END OF FILE: 86400-customer-external-libraries/.gitkeep ---

--- START OF FILE: 87000-customer-internal-executables/.gitkeep ---
--- END OF FILE: 87000-customer-internal-executables/.gitkeep ---

--- START OF FILE: 87000-internal/.gitkeep ---
--- END OF FILE: 87000-internal/.gitkeep ---

--- START OF FILE: 87000-internal-toolchains-source/.gitkeep ---
--- END OF FILE: 87000-internal-toolchains-source/.gitkeep ---

--- START OF FILE: 87100-customer-internal-toolchains/.gitkeep ---
--- END OF FILE: 87100-customer-internal-toolchains/.gitkeep ---

--- START OF FILE: 87200-customer-internal-adapters/.gitkeep ---
--- END OF FILE: 87200-customer-internal-adapters/.gitkeep ---

--- START OF FILE: 87300-customer-internal-actors/.gitkeep ---
--- END OF FILE: 87300-customer-internal-actors/.gitkeep ---

--- START OF FILE: 87400-customer-internal-libraries/.gitkeep ---
--- END OF FILE: 87400-customer-internal-libraries/.gitkeep ---

--- START OF FILE: 87500-customer-cognitive-assistance/.gitkeep ---
--- END OF FILE: 87500-customer-cognitive-assistance/.gitkeep ---

--- START OF FILE: 87600-customer-platform-delivery/.gitkeep ---
--- END OF FILE: 87600-customer-platform-delivery/.gitkeep ---

--- START OF FILE: 87700-customer-execution-contexts/.gitkeep ---
--- END OF FILE: 87700-customer-execution-contexts/.gitkeep ---

--- START OF FILE: 88000-customer-templates/.gitkeep ---
--- END OF FILE: 88000-customer-templates/.gitkeep ---

--- START OF FILE: 88000-internal-libraries-source/.gitkeep ---
--- END OF FILE: 88000-internal-libraries-source/.gitkeep ---

--- START OF FILE: 89000-archived-logic/.gitkeep ---
--- END OF FILE: 89000-archived-logic/.gitkeep ---

--- START OF FILE: 89000-customer-product-plane/.gitkeep ---
--- END OF FILE: 89000-customer-product-plane/.gitkeep ---

--- START OF FILE: 89000-internal-actors-source/.gitkeep ---
--- END OF FILE: 89000-internal-actors-source/.gitkeep ---

--- START OF FILE: 90000-authority/.gitkeep ---
--- END OF FILE: 90000-authority/.gitkeep ---

--- START OF FILE: 90000-enablement-labs/.gitkeep ---
--- END OF FILE: 90000-enablement-labs/.gitkeep ---

--- START OF FILE: 90100-rehydration-seed/.gitkeep ---
--- END OF FILE: 90100-rehydration-seed/.gitkeep ---

--- START OF FILE: 91000-ext-artifact-cognition-intelligence/.gitkeep ---
--- END OF FILE: 91000-ext-artifact-cognition-intelligence/.gitkeep ---

--- START OF FILE: 91000-external-executables/.gitkeep ---
--- END OF FILE: 91000-external-executables/.gitkeep ---

--- START OF FILE: 91200-external-adapters/.gitkeep ---
--- END OF FILE: 91200-external-adapters/.gitkeep ---

--- START OF FILE: 92000-external-toolchains/.gitkeep ---
--- END OF FILE: 92000-external-toolchains/.gitkeep ---

--- START OF FILE: 92000-grammar-visualizers/.gitkeep ---
--- END OF FILE: 92000-grammar-visualizers/.gitkeep ---

--- START OF FILE: 93000-external-libraries/.gitkeep ---
--- END OF FILE: 93000-external-libraries/.gitkeep ---

--- START OF FILE: 94000-external-actors/.gitkeep ---
--- END OF FILE: 94000-external-actors/.gitkeep ---

--- START OF FILE: 94000-internal-actors/.gitkeep ---
--- END OF FILE: 94000-internal-actors/.gitkeep ---

--- START OF FILE: 95000-authority/.gitkeep ---
--- END OF FILE: 95000-authority/.gitkeep ---

--- START OF FILE: 95000-enablement-labs/.gitkeep ---
--- END OF FILE: 95000-enablement-labs/.gitkeep ---

--- START OF FILE: 95000-projection/.gitkeep ---
--- END OF FILE: 95000-projection/.gitkeep ---

--- START OF FILE: 95100-rehydration-seed/.gitkeep ---
--- END OF FILE: 95100-rehydration-seed/.gitkeep ---

--- START OF FILE: 96000-int-artifact-cognition-intelligence/.gitkeep ---
--- END OF FILE: 96000-int-artifact-cognition-intelligence/.gitkeep ---

--- START OF FILE: 96000-internal-executables/.gitkeep ---
--- END OF FILE: 96000-internal-executables/.gitkeep ---

--- START OF FILE: 96200-internal-adapters/.gitkeep ---
--- END OF FILE: 96200-internal-adapters/.gitkeep ---

--- START OF FILE: 97000-internal-toolchains/.gitkeep ---
--- END OF FILE: 97000-internal-toolchains/.gitkeep ---

--- START OF FILE: 98000-internal-libraries/.gitkeep ---
--- END OF FILE: 98000-internal-libraries/.gitkeep ---

--- START OF FILE: 99000-internal-actors/.gitkeep ---
--- END OF FILE: 99000-internal-actors/.gitkeep ---

--- START OF FILE: 99000-internal-actors/s-natives ---
; Sealed Synthesized Output
; Mode: DYNAMIC
; Engine: go-compiler

--- END OF FILE: 99000-internal-actors/s-natives ---

--- START OF FILE: 99000-internal-libraries/.gitkeep ---
--- END OF FILE: 99000-internal-libraries/.gitkeep ---

--- START OF FILE: 99900-metabolic-projection/.gitkeep ---
--- END OF FILE: 99900-metabolic-projection/.gitkeep ---

